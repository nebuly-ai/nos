apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ include "gpu-partitioner.fullname" . }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ include "gpu-partitioner.fullname" . }}
subjects:
  - kind: ServiceAccount
    name: {{ include "gpu-partitioner.fullname" . }}
    namespace: {{ .Release.Namespace }}
