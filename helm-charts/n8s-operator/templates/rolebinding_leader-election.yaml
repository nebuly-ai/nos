apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ include "operator.name" . }}-leader-election
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: {{ include "leaderElectionRoleName" . }}
subjects:
  - kind: ServiceAccount
    name: {{ include "operator.name" . }}
